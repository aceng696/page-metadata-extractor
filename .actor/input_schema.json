{
  "title": "Page Metadata Extractor Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "url": {
      "title": "Website URL",
      "type": "string",
      "description": "Full URL of the page to extract metadata from (e.g. https://example.com)",
      "editor": "textfield",
      "prefill": "https://example.com"
    }
  },
  "required": ["url"]
}
